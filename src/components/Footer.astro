---
const quickLinks = [
  { href: '/about', label: 'Start Here' },
  { href: '/articles', label: 'Articles' },
  { href: '/hire', label: 'Hire me' },
  { href: '/newsletter', label: 'Sunday Newsletter' },
  { href: 'https://twitter.com/kadlac', label: 'DM me on X', external: true },
  { href: 'https://cal.com/kadlac/discovery-call', label: 'Coaching', external: true },
  { href: '/now', label: 'Now' },
];

const products = [
  { href: '/my', label: 'My Free Creative Toolkit', underline: true },
  { href: 'https://approachabledesign.co/courses/8020-design-challenge', label: 'The Creator Design Kit', external: true },
  { href: 'https://approachabledesign.co/products/design-vault', label: 'The Design Vault', external: true },
  { href: 'https://approachabledesign.co/products/youtube-masterclass', label: 'YouTube Thumbnail Design', external: true },
  { href: 'https://writersdraw.com', label: "Writer's Draw", external: true },
];

const currentYear = new Date().getFullYear();
---

<footer class="bg-brand-brown text-white mt-auto">
  <div class="max-w-[1200px] mx-auto px-6 py-16">
    <div class="grid grid-cols-1 md:grid-cols-12 gap-12 md:gap-8">
      <!-- Bio Column -->
      <div class="md:col-span-5">
        <p class="text-xl leading-relaxed">
          <strong class="font-bold">I'm Nate Kadlac:</strong> independent designer, instructor, and entrepreneur.
        </p>
        <p class="mt-6 text-white/80 leading-relaxed">
          I believe the ability to communicate visually, tell stories with copy, or connect with branding should be approachable to everyone.
        </p>
        <p class="mt-6 text-white/80 leading-relaxed">
          The world is a better place when we can confidently reflect our inner taste with the world around us.
        </p>
      </div>

      <!-- Quick Links -->
      <div class="md:col-span-3 md:col-start-7">
        <h4 class="font-heading font-semibold text-sm uppercase tracking-wider mb-6 text-brand-yellow">
          Quick Links
        </h4>
        <ul class="space-y-4">
          {quickLinks.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-white/80 hover:text-white transition-colors"
                target={link.external ? '_blank' : undefined}
                rel={link.external ? 'noopener noreferrer' : undefined}
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Products & Services -->
      <div class="md:col-span-3">
        <h4 class="font-heading font-semibold text-sm uppercase tracking-wider mb-6 text-brand-yellow">
          Products & Services
        </h4>
        <ul class="space-y-4">
          {products.map((link) => (
            <li>
              <a
                href={link.href}
                class:list={[
                  "text-white/80 hover:text-white transition-colors",
                  link.underline && "underline"
                ]}
                target={link.external ? '_blank' : undefined}
                rel={link.external ? 'noopener noreferrer' : undefined}
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Copyright -->
    <div class="mt-16">
      <p class="text-sm text-white/50">
        © {currentYear} Approachable Design LLC. All rights reserved.
      </p>
    </div>
  </div>
</footer>
